import{json,supabase}from'./_lib.js';export default async function handler(req,res){try{if(req.method==='GET'){const items=await supabase('release_candidates?status=eq.pending&select=*&order=discovered_at.desc');return json(res,200,{items})}if(req.method==='POST'){const{id,status}=req.body||{};if(!id||!['approved','ignored'].includes(status))return json(res,400,{error:'Invalid review'});const item=await supabase(`release_candidates?id=eq.${encodeURIComponent(id)}`,{method:'PATCH',body:{status,reviewed_at:new Date().toISOString()}});return json(res,200,{item})}json(res,405,{error:'Method not allowed'})}catch(e){json(res,500,{error:e.message})}}